package week8;
// We created interface Animal.
public interface Animal {
    void eat(); // Interface abstract methods can not have body. final can not use, because in that case we can not override the method.
    void drink();

}
